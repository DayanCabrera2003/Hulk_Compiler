// iterables.hulk — uso de iterables vía el protocolo Iterable y el tipo Range.
// Iterable y Range se definen en prelude/prelude.hulk (Hulk.md §15).
// El driver los prepende automáticamente antes de parsear el fuente del usuario.
for (x in new Range(0, 5)) print(x);
